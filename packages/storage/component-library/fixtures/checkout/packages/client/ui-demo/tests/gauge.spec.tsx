// Fixture mount call: the scanner lifts the first <Gauge ...> element as the
// record's usage example. Never executed (not a vitest-collected path).
const sample = <Gauge label="Sessions" value={3} hint="this week" />
void sample
